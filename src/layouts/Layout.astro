---
import '../styles/global.css';

interface Props {
  title?: string;
}

const { title = 'Mullet - Wedding Event' } = Astro.props;
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="generator" content={Astro.generator} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>{title}</title>
  </head>
  <body class="font-[family-name:var(--font-montserrat)] antialiased">
    <slot />
  </body>
</html>
